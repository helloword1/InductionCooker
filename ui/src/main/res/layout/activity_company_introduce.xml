<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_company_introduce"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="it.gk.environmentalprotectionbox.SetFragment.CompanyIntroduceActivity">
    <RelativeLayout
        android:id="@+id/rl_ComIntroduce"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:layout_alignParentTop="true"
        android:layout_gravity="center"
        android:background="@color/colorThem"
        >
        <Button
            android:id="@+id/bt_ComIntroduceBack"
            android:paddingLeft="20dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:drawableLeft="@mipmap/back"
            android:background="@color/colorCusTrans"
            />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_centerInParent="true"
            android:textSize="20sp"
            android:text="公司简介"
            android:textColor="@color/colorWhite"
            />
    </RelativeLayout>

    <WebView
        android:id="@+id/web_View"
        android:layout_below="@+id/rl_ComIntroduce"
        android:layout_width="match_parent"
        android:layout_height="match_parent"></WebView>
    <ProgressBar
        android:id="@+id/pb_ProgressBar"
        android:layout_centerInParent="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
</RelativeLayout>
